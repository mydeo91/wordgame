import React from "react";
import BackButton from "../../components/Button/Back";

export const TeamPage = props => {
  return (
    <div>
      <p>손현수</p>
      <p>이은호</p>
      <BackButton {...props} />
    </div>
  );
};
